# Build output — regenerated by `make build`. Published to gh-pages, not main.
/dist/

# App Store Connect upload assets — generated locally from raw/, not used by the site.
apps/*/screenshots/appstore/

.DS_Store
